# RUN ON YOUR MACHINE:

### Necessary packages & modules
Tkinter, Google, Random, Pymongo, Dotenv

### Create a .env file: 
Specify your Gemini API key under the variable → **API_KEY**
Specify your MongoURI connection tring → **MONGU_URI**

*Since it is entirely terminal based, you can run it locally!*